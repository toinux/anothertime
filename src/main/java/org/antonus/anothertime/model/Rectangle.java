package org.antonus.anothertime.model;

import java.awt.*;

public class Rectangle extends Draw {
    public Rectangle(int x, int y, int w, int h, Color cl) {
        super("dr", cl, x, y, w, h);
    }
}
