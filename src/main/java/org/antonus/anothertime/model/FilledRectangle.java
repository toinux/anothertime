package org.antonus.anothertime.model;

import java.awt.*;

public class FilledRectangle extends Draw {
    public FilledRectangle(int x, int y, int w, int h, Color cl) {
        super("df", cl, x, y, w, h);
    }
}
